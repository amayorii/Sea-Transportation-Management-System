<Window x:Class="Sea_Transportation_Management_System.View.PortItemSelecting"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sea_Transportation_Management_System.View"
        mc:Ignorable="d"
        Title="PortItemSelectingWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="StorageList"
                 Margin="5"
                 SelectionChanged="Storage_SelectionChanged"
                 SelectionMode="Extended"/>

        <Button Content="Submit"
                x:Name="Submit" Grid.Row="1"
                Height="30" Width="90"
                IsEnabled="False"
                Click="SubmitClick"
                HorizontalAlignment="Right"
                Margin="0,0,10,0"/>
    </Grid>
</Window>
